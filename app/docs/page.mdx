import Link from 'next/link';

import { Castle, Github, Twitter } from 'lucide-react';

import CredentialFeature from './(components)/credential-feature';
import LinkFeature from './(components)/link-feature';
import PuzzleFeature from './(components)/puzzle-feature';
import Description from '@/components/templates/mdx/description';
import { getBaseMetadata } from '@/lib/utils';

export const metadata = getBaseMetadata({
  title: 'Curta Docs',
  titleTemplate: '%s | Curta Docs',
});

# Curta Documentation
<Description children='Curta is a talent discovery competition platform and community of protocol engineers.' />

## Credentials
<div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-1 gap-4 lg:grid-cols-3">
  {[
    {
      name: 'Puzzles',
      description: 'A CTF protocol, where players create and solve EVM puzzles to earn NFTs.',
      href: '/docs/puzzles/overview',
      children: <PuzzleFeature />
    }
  ].map((item) => <CredentialFeature key={item.href} {...item} />)}
</div>

## Links

Related links to Curta.

<div className="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-1 gap-4 lg:grid-cols-3">
  {[
    {
      name: 'waterfall-mkt/curta',
      description: 'CTF protocol contracts.',
      href: 'https://github.com/waterfall-mkt/curta',
      icon: <Github />
    },
    {
      name: 'waterfall-mkt/curta.wtf',
      description: 'Website source.',
      href: 'https://github.com/waterfall-mkt/curta.wtf',
      icon: <Github />
    },
    {
      name: '@curta_ctf',
      href: 'https://github.com/waterfall-mkt/curta',
      icon: <Twitter />
    },
    {
      name: 'Builder stream',
      description: 'Curta contracts',
      href: 'https://optimizooor-streams.buidlguidl.com/',
      icon: <Castle />
    },
    {
      name: 'waterfall-mkt/curta.wtf',
      description: 'Curta contracts',
      href: 'https://github.com/waterfall-mkt/curta.wtf',
      icon: <Github />
    },
    {
      name: 'waterfall-mkt/curta',
      description: 'Curta contracts',
      href: 'https://github.com/waterfall-mkt/curta',
      icon: <Github />
    }
  ].map((item) => <LinkFeature key={item.href} {...item} />)}
</div>
